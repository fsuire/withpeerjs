@import 'icons';

prefix-icon {
  display: flex;
  flex-direction: row;
  align-items: center;

  &[icon="icon-pencil"] {
    add-icon($icon-pencil);
  }
  
  &:before {
    cursor: default;
    flex-shrink: 0;
  }
  
  > * {
    flex: 1;
  }

}