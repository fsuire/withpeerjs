(function() {
  'use strict';

  // module.exports = webrtcRoute;
  //
  // webrtcRoute['@singleton'] = true;
  // webrtcRoute['@require'] = ['services/config'];
  //
  // function webrtcRoute(config) {
  //
  //   return function(application) {
  //     application.get('/webrtc', function(req, res) {
  //       if(req.headers.accept.indexOf('application/json') >= 0) {
  //         res.setHeader('Content-Type', 'application/json');
  //         var response = {
  //           domain: req.params.domain,
  //           name: req.params.name,
  //           id: req.params.id
  //         };
  //         res.send(JSON.stringify(response));
  //       } else {
  //         res.status(406);
  //         res.send('Oooups ! Error 406 ! "Not Acceptable" !');
  //       }
  //     });
  //   };
  //
  // }

})();
